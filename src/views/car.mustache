<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Document</title>
</head>
<body>
	<h1>{{car.title}}</h1>
	<img src="{{car.image}}" alt="{{car.title}}" />
	<p>Price: {{car.price}}$</p>
	<p>Colors: {{car.colors}}</p>
	<p>{{car.description}}</p>
	<nav>
  	<a href="/">Home</a> / <a href="/category/{{category.id}}">{{car.category.title}}</a> / {{car.title}}
	</nav>
</body>
</html>